{% extends 'baseAdmin.html.twig' %}

{% block title %}CategoriesHome{% endblock %}

{% block body %}
    <h1>CategoriesHome</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categories_home.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categories_home.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ categories_home.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categories_home.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_categories_home_index') }}">back to list</a>

    <a href="{{ path('app_admin_categories_home_edit', {'id': categories_home.id}) }}">edit</a>

    {{ include('admin/categories-home/_delete_form.html.twig') }}
{% endblock %}
